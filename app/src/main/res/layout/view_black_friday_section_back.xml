<?xml version="1.0" encoding="utf-8"?>

<!--suppress XmlUnusedNamespaceDeclaration -->
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="laurcode.com.infiniteimagescroller.startup.viewmodel.SplashViewModel" />

    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        android:paddingTop="@dimen/padding_bigger"
        android:paddingBottom="@dimen/padding_bigger">

        <TextView
            android:id="@+id/black_friday_deals_heading"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            style="@style/TextStyle.Cabin.Title.Dark"
            android:text="@string/black_friday_deals"
            android:paddingBottom="@dimen/padding_medium"
            android:visibility="invisible"
            tools:visibility="visible" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_horizontal"
            tools:ignore="RtlSymmetry">

            <TextView
                android:id="@+id/black_friday_link_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                style="@style/TextStyle.Cabin.Caption.Medium"
                android:text="@string/black_friday_url_hint"
                android:paddingEnd="@dimen/padding_small"
                android:visibility="invisible"
                tools:visibility="visible"/>

            <TextView
                android:id="@+id/black_friday_deals_link"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.Link.Medium"
                android:text="@{viewModel.blackFridayUrl}"
                tools:text="www.google.com"
                android:autoLink="web"
                android:visibility="invisible"
                android:clickable="true"
                android:focusable="true"
                tools:visibility="visible"/>

        </LinearLayout>

    </LinearLayout>

</layout>